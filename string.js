//string create with two ways.
let str= 'you and  My name  ';
var value=new String('what are the functions in string');
str.charAt(1)
value.charCodeAt(2)
str.concat(value)
str.lastIndexOf('m')
str.search('n')
value.match('a')
str.replace('my','you')
str.split()
str.trimStart()
str.trimEnd()
console.log();



/* 
    methods in string
    toLowercase()
    touppercase()
    charAt(index)
    charCodeAt(index) => it return the unicode of specified index.
    concat() =>join two strings.
    indexof('char') => return given character index.
    lastindexof('char') => return element index from last.
    search('char')=> index of char
    match()
    replace(old,new)
    trim()
    split()
    slice(start,end)
    substring(start,end);
    substr(start,end);
    str.padstart(fromindex number,"value");
    
*/